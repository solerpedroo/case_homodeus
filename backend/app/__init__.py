"""HomoDeus Labor Law Q&A Agent — backend package root.

EN:
    Marks this directory as the Python package `app`. The `__version__`
    string is exposed for health checks and logging. All application code
    (API, agent, retrieval, evaluation) lives under this package.

PT:
    Marca este diretório como o pacote Python `app`. A string `__version__`
    é usada em health checks e logs. Todo o código da aplicação (API,
    agente, recuperação, avaliação) vive sob este pacote.
"""
__version__ = "1.0.0"
