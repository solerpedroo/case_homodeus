"""HTTP API package.

EN:
    Groups FastAPI routers (`routes/`) that expose REST and SSE endpoints.
    The main app in `main.py` mounts these routers under prefixes such as
    `/chat` and `/eval`.

PT:
    Agrupa os routers FastAPI (`routes/`) que expõem REST e SSE. A
    aplicação principal em `main.py` monta estes routers sob prefixos como
    `/chat` e `/eval`.
"""
