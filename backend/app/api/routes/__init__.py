"""HTTP route modules.

EN:
    Each submodule (`chat`, `eval`) defines an `APIRouter` instance. Import
    side effects are minimal; routers are registered explicitly in `main.py`.

PT:
    Cada submódulo (`chat`, `eval`) define uma instância `APIRouter`. Os
    efeitos ao importar são mínimos; os routers são registados explicitamente
    em `main.py`.
"""
