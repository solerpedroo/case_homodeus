/**
 * EN: Chat route — thin wrapper mounting the streaming chat shell.
 * PT: Rota do chat — invólucro fino que monta o chat com streaming.
 */
import { ChatContainer } from "@/components/chat/ChatContainer";

export default function ChatPage() {
  return <ChatContainer />;
}
