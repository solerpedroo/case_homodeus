/**
 * EN: Eval route — benchmark dashboard (v1 vs v2 metrics, run buttons).
 * PT: Rota de avaliação — dashboard de benchmarks (métricas v1 vs v2).
 */
import { EvalDashboard } from "@/components/eval/EvalDashboard";

export default function EvalPage() {
  return <EvalDashboard />;
}
